services:
  - type: web
    name: narabote-live
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: NARABOTE_DATA_DIR
        sync: false
    plan: free
